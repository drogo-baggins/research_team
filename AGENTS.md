# AGENTS.md — AI開発エージェントへの指示

このファイルはAIエージェント（Copilot, Claude等）がこのリポジトリで作業する際に従うべきルールを定めます。

---

## リグレッション修正プロセス（全バグ修正・全リグレッション対応 必須）

バグ・リグレッションを修正する場合、**以下の手順を順序通りに必ず実行すること。省略禁止。**

```
1. 失敗するテストを書く
   - 現在の問題を再現するユニットテストを tests/unit/ に追加する
   - テストは「この動作が保証されるべき」という仕様を表現する

2. テストが FAIL することを確認する
   - pytest tests/unit/ -x -q を実行し、新しいテストが FAILED であることを出力で確認する
   - FAIL を確認してから初めて修正に進む

3. 本体コードを修正する

4. テストが PASS することを確認する
   - pytest tests/unit/ -x -q を実行し、全テストが PASSED であることを出力で確認する

5. コミットする（テスト出力を必ず確認してから）
```

### このプロセスが必要な理由

- テスト先行（Red → Green）により、修正が本当に問題を解決しているかを客観的に検証できる
- テストがコミット時に自動実行（pre-commit フック）されるため、将来の変更で同じリグレッションが再発した場合に即座に検知できる
- 「直したと思ったが実は別の箇所だった」という虚偽完了を防ぐ

### 違反の例（禁止）

- テストを書かずに修正して「完了」と報告する
- テストを書いたが FAIL 確認をせずに修正する
- テストは PASS したが pre-commit フックを確認しない

---

## コミット時の自動ガード（pre-commit フック）

`.git/hooks/pre-commit` が設置されており、コミット時に `pytest tests/unit/ -x -q` を自動実行する。
テストが1件でも失敗した場合、コミットはブロックされる。

### 新規クローン時のフック設置

```powershell
# Windows (PowerShell)
.\scripts\setup_hooks.ps1
```

フックのソースは `scripts/pre-commit.hook` に管理されている。

---

## 完了条件（絶対厳守）

コード変更・バグ修正・機能追加のいずれであっても、**「完了」「修正しました」「対応済み」と報告する前に、必ず以下を実行すること。**

### 必須ステップ

```
1. pytest tests/unit/ -x -q を実行する
2. 出力を読み、失敗が 0 件であることを目視確認する
3. 確認した出力結果をレスポンスに含める
4. 失敗が1件でもあれば「完了」と言わない
```

### 許可されない報告パターン

以下の言い方は、テスト実行・確認なしに使用してはならない：

- 「修正しました」「対応しました」「直しました」
- 「完了です」「done」「fixed」
- 「動くはずです」「問題ないと思います」

テストを実行した証拠（出力）なしにこれらを使った場合、その報告は虚偽とみなす。

---

## テスト実行コマンド

```bash
# ユニットテスト（通常はこれだけでよい）
pytest tests/unit/ -x -q

# 全テスト
pytest -x -q
```

---

## 自動検証できないコンポーネント（手動確認が必要）

以下のファイルを変更した場合、ユニットテストが通過しても **「完了」と報告してはならない**。
必ず「手動スモークテストが未実施」と明記し、人間に確認を求めること。

### 対象ファイル

| ファイル | 理由 | 手動確認方法 |
|---|---|---|
| `src/research_team/search/human.py` | 実ブラウザ・人間操作が必要 | 下記スモークテスト手順を参照 |
| `src/research_team/pi_bridge/client.py` | 実際のpi-agentプロセス起動が必要 | 実際にエージェントを起動して動作確認 |
| `src/research_team/pi_bridge/web_search.ts` | Node.jsプロセス・外部通信が必要 | 実際にエージェントを起動して動作確認 |

### HumanSearchEngine スモークテスト手順

変更後、以下を人間が手動で実行・目視確認すること：

```
1. research-team コマンドを起動する
2. ブラウザウィンドウが開くことを確認する
3. 簡単な検索クエリを投げる（例：「テスト」）
4. Google検索ページが表示され、承認UIが機能することを確認する
5. 承認後、検索結果が返ってくることを確認する
```

### AIエージェントへの指示

上記ファイルを変更した場合、レスポンスに以下を必ず含めること：

```
⚠️ 手動確認が必要：[変更したファイル名] を変更しました。
ユニットテストはパスしましたが、実ブラウザ動作の確認は自動化できません。
スモークテスト手順に従って動作を確認してください。
```

---

## テンプレートファイル変更時の追加確認

`*.template` ファイルを変更した場合、以下も確認すること：

- `str.format()` で使われるプレースホルダー（`{...}`）を追加・変更した場合、既知のキー以外がないか確認する
- 意図しないプレースホルダーは `{{...}}` にエスケープする
- 変更後に必ずユニットテストを実行する

コード変更・バグ修正・機能追加のいずれであっても、**「完了」「修正しました」「対応済み」と報告する前に、必ず以下を実行すること。**

### 必須ステップ

```
1. pytest tests/unit/ -x -q を実行する
2. 出力を読み、失敗が 0 件であることを目視確認する
3. 確認した出力結果をレスポンスに含める
4. 失敗が1件でもあれば「完了」と言わない
```

### 許可されない報告パターン

以下の言い方は、テスト実行・確認なしに使用してはならない：

- 「修正しました」「対応しました」「直しました」
- 「完了です」「done」「fixed」
- 「動くはずです」「問題ないと思います」

テストを実行した証拠（出力）なしにこれらを使った場合、その報告は虚偽とみなす。

---

## テスト実行コマンド

```bash
# ユニットテスト（通常はこれだけでよい）
pytest tests/unit/ -x -q

# 全テスト
pytest -x -q
```

---

## 自動検証できないコンポーネント（手動確認が必要）

以下のファイルを変更した場合、ユニットテストが通過しても **「完了」と報告してはならない**。
必ず「手動スモークテストが未実施」と明記し、人間に確認を求めること。

### 対象ファイル

| ファイル | 理由 | 手動確認方法 |
|---|---|---|
| `src/research_team/search/human.py` | 実ブラウザ・人間操作が必要 | 下記スモークテスト手順を参照 |
| `src/research_team/pi_bridge/client.py` | 実際のpi-agentプロセス起動が必要 | 実際にエージェントを起動して動作確認 |
| `src/research_team/pi_bridge/web_search.ts` | Node.jsプロセス・外部通信が必要 | 実際にエージェントを起動して動作確認 |

### HumanSearchEngine スモークテスト手順

変更後、以下を人間が手動で実行・目視確認すること：

```
1. research-team コマンドを起動する
2. ブラウザウィンドウが開くことを確認する
3. 簡単な検索クエリを投げる（例：「テスト」）
4. Google検索ページが表示され、承認UIが機能することを確認する
5. 承認後、検索結果が返ってくることを確認する
```

### AIエージェントへの指示

上記ファイルを変更した場合、レスポンスに以下を必ず含めること：

```
⚠️ 手動確認が必要：[変更したファイル名] を変更しました。
ユニットテストはパスしましたが、実ブラウザ動作の確認は自動化できません。
スモークテスト手順に従って動作を確認してください。
```

---

## テンプレートファイル変更時の追加確認

`*.template` ファイルを変更した場合、以下も確認すること：

- `str.format()` で使われるプレースホルダー（`{...}`）を追加・変更した場合、既知のキー以外がないか確認する
- 意図しないプレースホルダーは `{{...}}` にエスケープする
- 変更後に必ずユニットテストを実行する
